const Toolbar = () => {
  return (
    <nav className="navbar navbar-dark bg-warning">
      <div className="container-fluid">
        <span className="navbar-brand" style={{color: "black"}}>User</span>
      </div>
    </nav>
  );
};

export default Toolbar;